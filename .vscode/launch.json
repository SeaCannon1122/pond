{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug my_program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/test/pond_test",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "env": {
                "POND_BUNDLE_PATH": "${workspaceFolder}/build/modules"
            }
        }
    ]
}